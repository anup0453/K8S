apiVersion : v1
kind : Pod
metadata :
 name : my-pod
spec :
        containers :
          - name : c00
            image : nginx
          - name : c01
            image : httd
            ports :
              - containerPort : 80
