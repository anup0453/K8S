apiVersion : v1
kind : Service
metadata :
 name : nodeport
 labels :
  app : my-app
spec :
 type : NodePort
 selector :
  app : my-app
 ports :
  - port : 80
    targetPort : 80
~                    
